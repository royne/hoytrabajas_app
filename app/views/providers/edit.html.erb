<h1><%= t('provider.edit_provider') %></h1>

<%= render "form", provider: @provider %>

<br>

<div>
  <%= link_to t('provider.show_provider'), @provider %> |
  <%= link_to t('provider.back_providers'), providers_path %>
</div>
